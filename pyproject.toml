[project]
name = "pm-discovery-accelerator"
version = "0.1.0"
description = "Autonomous PM Problem-Discovery Accelerator"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.14.0",
    "pydantic>=2.0.0",
    "python-dotenv>=1.0.0",
    "pyyaml>=6.0.0",
]
